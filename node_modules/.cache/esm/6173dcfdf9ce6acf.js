let bodyParser,mongoose,express,SeedDB,Campground;_f9c‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_f9c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_f9c‍.w("express",[["default",["express"],function(v){express=v}]]);_f9c‍.w("./seeds",[["default",["SeedDB"],function(v){SeedDB=v}]]);_f9c‍.w("./views/models/campground",[["default",["Campground"],function(v){Campground=v}]]);// Node.js body parsing middleware

// Mongoose

const dbRoute = 'mongodb+srv://enkienki:Rmn2010%2a@cluster0-wluuq.mongodb.net/yelpCamp?retryWrites=true&w=majority'
// Express

const app = express(),
port = 3000


SeedDB()

// APP CONFIG
// mongoose
mongoose.connect(dbRoute, { useNewUrlParser: true })
let db = mongoose.connection
db.once('open', () => console.log('connected to database'))
// body parser
app.use(bodyParser.urlencoded({ extended: true }))
// used to route the app.css to public folder
app.use(express.static('public'))
// allow to not use the extension .ejs after file names
app.set('view engine', 'ejs')



//================================

//MODELS


//================================

//ROUTES

// Route to homepage
app.get('/', (req, res) => res.render('home'))

// INDEX - show all campgrounds
app.get('/campgrounds', (req, res) => {
    //retrive existing campgrounds from DB
    Campground.find({}, (err, campgrounds) => {
        err ? 
        _f9c‍.g.console.log(err)
        : 
        //display campgrounds from DB
        res.render('index', { campgrounds: campgrounds })   
    })   
})

// NEW - show form to create new campground
app.get('/new', (req, res) => res.render('new'))

// CREATE - add new campground to the DB coming from the form
app.post('/addNewCamp', (req, res) => {
    Campground.create({
        name : req.body.newCampName,
        image: req.body.newCampImage,
        description: req.body.newCampDescription,
    },
        (err, camp) => { err ? _f9c‍.g.console.log(err) : _f9c‍.g.console.log(camp) })
    res.redirect('/index')
})

// SHOW - show infos about one campground
app.get('/campgrounds/:id', (req, res) => {
    Campground.findById(req.params.id, (err, foundCampground) => {
        if(err) {
            _f9c‍.g.console.log(err)
        } else {
            res.render('show', { campground: foundCampground })            
        }
    })
})

//================================

app.listen(port, () => console.log(`Express listen on ${port}`))